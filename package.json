{
  "name": "exclude-vat-calculator",
  "version": "1.0.0",
  "description": "VAT Calculator, Exclude VAT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "copy": "copyfiles -u 1 src/**/**/* src/**/* src/* build",
    "prod": "NODE_ENV=production npx tailwindcss -i tailwind.css -o src/css/style.css --minify && npx uglifyjs calculator.js -o ./src/js/calc.min.js --source-map && npm run copy && workbox injectManifest workbox-config.js"
  },
  "keywords": ["vat", "calculator", "exclude"],
  "author": "KRAFTBIT",
  "license": "MIT",
  "devDependencies": {
    "copyfiles": "^1.2.0",
    "tailwindcss": "^2.2.7",
    "uglify-js": "^3.14.1",
    "workbox-cli": "^3.5.0"
  }
}
